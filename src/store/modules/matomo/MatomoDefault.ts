import { MatomoInterface } from '@/models/interfaces/MatomoInterface';

const MatomoDefault: MatomoInterface = {
  actionsBeforeProjectClicked: 0,
  showGDPRModal: false,
};

export default MatomoDefault;
